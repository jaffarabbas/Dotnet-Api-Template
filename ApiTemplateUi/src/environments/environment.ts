export const environment = {
  production: false,
  // For local development without Docker (ng serve), use:
  apiUrl: 'http://localhost:8080/api/v1'
  // For Docker development (docker-compose up), use:
  // apiUrl: 'http://localhost/api/v1'
};
